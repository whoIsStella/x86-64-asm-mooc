.intel_syntax noprefix
.text
.globl sub_negate
.type sub_negate, @function
sub_negate:
    # TODO: return a - b.  a is in rdi, b is in rsi, result goes in rax.
    # This stub returns 0 so all tests start RED.  Replace with the real body.
    mov rax, 0
    ret
.size sub_negate, .-sub_negate
